
server:
  port: 9090
spring:
  application:
    name: USER-SERVICE
  datasource:
    url: jdbc:mysql://localhost:3306/hostel_users
    username: root
    password: "258036bm,"
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect
  eureka:
    client:
      service-url:
        defaultZone: http://localhost:8761/eureka