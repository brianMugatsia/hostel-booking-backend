server:
  port: 9090

spring:
  application:
    name: USER-SERVICE

  datasource:
    url: jdbc:mysql://localhost:3306/hostel_users?useSSL=false&serverTimezone=UTC
    username: root
    password: 258036bm,
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update   # Now JPA can manage schema if needed
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

  # Flyway removed completely
  # flyway:
  #   enabled: true
  #   locations: classpath:db/migration

jwt:
  
  secret: mySuperSuperSecretKeyThatIsLongEnough1234

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8761/eureka

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
  health:
    db:
      enabled: true
    diskspace:
      enabled: true